<h1>Add a new cocktail</h1>

<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.submit "Submit Cocktail", class: "btn btn-primary w-100"%>
<% end %>

<%= link_to "Cancel", cocktails_path %>